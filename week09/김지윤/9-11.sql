# 틀린 코드
SELECT
    d.ID,
    d.EMAIL,
    d.FIRST_NAME,
    d.LAST_NAME
FROM
    DEVELOPERS AS d
LEFT JOIN
    SKILLCODES AS s
ON
    d.SKILL_CODE & s.CODE = s.CODE
WHERE
    CATEGORY = 'Front End'
ORDER BY
    ID